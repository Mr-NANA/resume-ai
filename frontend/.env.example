# frontend/.env.local
# Copy this file to .env.local — never commit .env.local to git

# Your Render backend URL (only needed for production on Vercel)
# Leave empty for local dev — Vite proxy handles /api → localhost:5000
VITE_API_URL=https://YOUR-APP-NAME.onrender.com
